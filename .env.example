# Copy to ".env" and adjust. (.env is gitignored.)

DATABASE_URL=postgresql://postgres:postgres@localhost:5432/loan_risk

# OpenAI (LLM 심사 코멘트 GET /api/v1/reviews)
# OPENAI_API_KEY=
# OPENAI_BASE_URL=
# LLM_MODEL=gpt-4o-mini

# 앱 로그(app.*): INFO(기본) | DEBUG(상세). DEBUG면 라우터/서비스 로그가 콘솔에 더 많이 찍힘.
# LOG_LEVEL=INFO
# LOG_LEVEL=DEBUG
